# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ChangeSite' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ChangeSite
  pod 'FSCalendar'

  target 'ChangeSiteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ChangeSiteUITests' do
    # Pods for testing
  end

end

target 'ChangeSiteNotificationService' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ChangeSiteNotificationService

end
